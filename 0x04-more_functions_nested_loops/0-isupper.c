#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: character
 * Return: 1 for uppercase and 0 otherwise
 */
int _isupper(int c)
{
	for (c = 'A' ; c <= 'Z' ; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
