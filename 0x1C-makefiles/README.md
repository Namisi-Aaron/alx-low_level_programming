This directory contains files for ALX SE project 0x1C. C - Makefiles

A Makefile is a special format file that contains a set of directives used by the make build automation tool to generate a target/goal
